{
  "name": "serverless-image-processing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.175.0",
    "eslint-config-blake": "^1.0.9",
    "serverless-helpers": "^0.0.6",
    "sharp": "^0.18.4"
  },
  "scripts": {
    "invoke:uploadFile": "sls invoke local --function uploadFile -p lib/mocks/uploadFile.json",
    "invoke:processImage": "sls invoke local --function processImage -p lib/mocks/processImage.json"
  }
}
